const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    fontFamily: {
        "sans": ['"Montserrat"'],
        "mono": ['monospace'],
    },
  },

  plugins: [],
};

module.exports = config;
